#include "holberton.h"

/**
 * _strlen - This function returns the length of a string
 * @*s: Our string
 *
 * Return: The length
 */

int _strlen(char *s);
{
	int a;
	int b = 0;

	for (a = 0; s[a] != '\0'; ++a)
		a++
			return (b);
}
