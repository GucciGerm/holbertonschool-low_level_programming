#include "holberton.h"

/**
 * puts2 - Function that prints 1 char out of 2 of a string
 * @str: This is our string that we will be using
 *
 */

void puts2(char *str)
{
	int a;

	for (a = 0; str[a] != 0; a++)
		if (x % 2 == 0)
			_putchar(str[a]);
	_putchar('\n');
}
