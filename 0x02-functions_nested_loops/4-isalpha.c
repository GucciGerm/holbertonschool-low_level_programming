#include "holberton.h"

/**
 * _isalpha - This function checks for alphabetic character
 * @c: C is a letter that would output lowercase and uppercase
 *
 * Return: 0 or 1
 */

int _isalpha(int c)
{

	if (c >= 'a' && c <= 'z')
	{ return (1);
	}
	{ return (0);
	}

}
