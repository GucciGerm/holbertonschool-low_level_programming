#include "holberton.h"

/**
 * _isalpha - This function checks for alphabetic character
 *
 * Return: 0 or 1
 */

int _isalpha(int c)
{

	if (c >= 'a' && c <= 'z')
	{ return (1);
	}
	{ return (0);
	}

}
