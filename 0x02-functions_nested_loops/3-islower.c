#include "holberton.h"

/**
 * _islower - This function checks for lowercased characters
 * @c: when returning 1 c is lowercase
 *
 * Return: 1 or 0
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{ return (1);
	}
	{ return (0);
	}

}
