#include "holberton.h"

/**
 * _islower - This function checks for lowercased characters
 *
 * Return: 1 or 0
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{ return (1);
	}
	{ return (0);
	}

}
